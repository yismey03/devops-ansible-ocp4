- name: Demostrar los módulos k8s
  hosts: localhost
  gather_facts: no
  connection: local
  module_defaults:
    group/k8s:
      namespace: "marianogbarbero-dev"
      api_key: "sha256~skEKvMh_aVQRJDE8l47MRZWrEdSoyeT9F_FNIUkwrdY"
      host: https://api.sandbox.x8i5.p1.openshiftapps.com:6443
      
  tasks:
    - name: Creamos los objetos del manifiesto
      k8s:
        state: present
        src: "{{ playbook_dir + '/hello.yml' }}"
        
    - name: Cambiar de ClusterIP a NodePort
      k8s_service:
        state: present
        name: nodejs-ex
        type: NodePort
        ports:
          - port: 8080
            protocol: TCP
            
    - name: Obtener información de los pods
      k8s_info:
        kind: Pod
        label_selectors:
          - app = nodejs-ex
          
    - name: Exponer el puerto https como ClusterIP
      k8s_service:
        state: present
        name: nodejs-ex
        ports:
          - port: 443
            protocol: TCP
        selector:
          app: nodejs-ex
        
